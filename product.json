{
  "$comment": "Product metadata contract. The agentos-website repo snapshots this at build time so version and install commands cannot drift. See _portfolio/WEBSITE_STANDARD.md.",
  "name": "AgentOS",
  "id": "agentos",
  "tagline": "Requested is not verified.",
  "version": "0.5.0",
  "status": "beta",
  "repository": "",
  "website": "",
  "install": [
    {
      "method": "pip-editable",
      "command": "python3.12 -m pip install -e ."
    }
  ],
  "license": "MIT",
  "supported": [
    "CLI",
    "HTTP API (loopback, unauthenticated)",
    "MCP stdio",
    "Python 3.11+",
    "embedded sqlite at $AGENTOS_HOME"
  ],
  "planned": [
    "MCP client adapter",
    "API authentication",
    "execution sandboxing",
    "OrgOS (separate project)"
  ]
}
